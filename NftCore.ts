import data from "./data.json";

class NftCore {
  constructor() {}

  public async createNewNft({
    name,
    tomint,
  }: {
    name: number;
    tomint: string;
  }) {}

  public async updateNft({ name, tomint }: { name: number; tomint: string }) {}
}
